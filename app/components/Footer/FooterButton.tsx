import Image from 'next/image';
import Link from 'next/link';

export default function FooterButton() {
  return (
    <button className="bg-white text-textColor px-4 py-2 rounded-md font-bold cursor-pointer mt-10">
      JOIN THE COMMUNITY
    </button>
  );
}
